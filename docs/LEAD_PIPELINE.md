# Lead Pipeline (Phase 6D)

See canonical statuses and transitions in `src/lib/registrationDomain.ts` and SQL `can_transition_lead_status`. Terminal without admin action: `converted` (via `convert_lead`) and `archived`.